/**
 * Minumum Clock Angle
 *
 * given a time(string), return the minimum angle between the hour and minute
 * hands of a 24-hour clock(number)
 *
 * examples:
 * minClockAngle.getMinAngle("6:00")  --> returns 90
 * minClockAngle.getMinAngle("18:00") --> returns 90
 * minClockAngle.getMinAngle("6:17")  --> returns 7.75
 * minClockAngle.getMinAngle("18:17") --> returns 172.25
 *
 */

var minClockAngle = {};

minClockAngle.getMinAngle = function(time){
    // your code here

    return time;
};

module.exports = minClockAngle;
